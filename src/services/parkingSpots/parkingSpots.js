class ParkingSpot {
    constructor(id, type, isOccupied) {
      this.id = id; // Unique identifier for the parking spot
      this.type = type; // Type of parking spot (e.g., motorcycle, car, bus)
      this.isOccupied = isOccupied; // Boolean indicating whether the spot is occupied or not
    }
  }